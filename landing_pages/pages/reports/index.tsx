import React from 'react';
import Layout from '../../components/Layout';

const Reports = ({toggleTheme, themeMode}) => {
    return(
        <Layout toggleTheme={toggleTheme} themeMode={themeMode}>
        <div>Reports PAGE</div>
        </Layout>
    )
}
export default Reports;